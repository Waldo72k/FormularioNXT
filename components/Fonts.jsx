// fontImports.jsx
import { Raleway } from 'next/font/google';
import { Lobster } from 'next/font/google';

export const texto = Lobster({ subsets: ['latin'], weight: '400' });
export const titulo = Raleway({ subsets: ['latin'], weight: '400' });
